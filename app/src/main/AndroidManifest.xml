<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fyp.srk.aiduneducateddeafdumb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TimeSigns.TimeSignActivity"></activity>
        <activity android:name=".Greetings.GreetingsActivity" />
        <activity android:name=".FamilyMembers.FamilyMembersActivity" />
        <activity android:name=".CountingNumbers.CountingNumbers" />
        <activity android:name=".EnglishAlphabets.EnglishAlphabetsActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>